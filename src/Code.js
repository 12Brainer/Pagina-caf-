/* ==========================================================================
   SAN BERNARDO SPECIALTY COFFEE ESTATE
   Archivo de compatibilidad.
   ==========================================================================
   El backend completo de Google Apps Script vive en el archivo `Code.gs`.
   Este archivo se conserva únicamente para mantener compatibilidad con el
   proyecto `.clasp` (rootDir = src). No contiene lógica duplicada.

   Para publicar: ejecuta `clasp push` o copia el contenido de `Code.gs`
   al editor de Apps Script.
   ========================================================================== */

// (Backend en Code.gs)

